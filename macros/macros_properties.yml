version: 2

macros:
  - name: get_payment_type_description
    description: >
      Returns the corresponding description for a payment_type.
    arguments:
      - name: payment_type
        type: int
        description: > 
          payment_type value.
          Must be one of the accepted values, otherwise the macro will return null